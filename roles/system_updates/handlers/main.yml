---
# System Updates Role - Handlers

- name: reboot system
  ansible.builtin.reboot:
    msg: "Rebooting for system updates"
    connect_timeout: 5
    reboot_timeout: 300
    pre_reboot_delay: 0
    post_reboot_delay: 30
  when: auto_reboot | default(false)
